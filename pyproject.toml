[project]
name = "redteam-ai-assist"
version = "0.1.0"
description = "LangGraph-based redteam coaching assistant for isolated cyber range labs."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
